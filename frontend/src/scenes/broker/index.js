import React from "react";
import Navbar from "../navbar";

const Broker = () => {
    return (
        <div>
            <Navbar />
            <h1>Broker</h1>
        </div>
    );
};

export default Broker;
